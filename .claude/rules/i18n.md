# Internationalization (i18n) Rules

- **Never hardcode strings** in the UI.
- Define all UI text as **constants in a separate place** and reference them from the components.
- **English only** is supported for now.
- Use **standard Angular pipes** (e.g. `CurrencyPipe`, `DecimalPipe`, `DatePipe`) for formatting currency, amounts, and similar values.
